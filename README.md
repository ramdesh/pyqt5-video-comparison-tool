# pyqt5-video-comparison-tool
Side by side comparison tool using PyQt5

### Installation and running

* Note that you need to have VLC media player installed.

```
pip install -r requirements.txt
fbs run
```
